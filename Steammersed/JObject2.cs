using Newtonsoft.Json;

namespace Steammersed
{
    internal class JObject2
    {
        /*
        public static SteamApi.root Parse(String requested_interface, String api_key, String? steamid = "")
        {
            var content = MainThing.steamRequestAsync(requested_interface + api_key + "&steamid=" + steamid).GetAwaiter().GetResult();
            SteamApi.root r = null;
            try
            {
                r = JsonConvert.DeserializeObject<SteamApi.root>(content);
            }
            catch (Exception ex)
            {
                Console.WriteLine(ex.Message);
            }
            return r;
        }*/
    }
}